{
  "scripts": {
    "build": "cd client && npm ci --prefer-offline --no-audit --no-fund && npm run build",
    "dev": "node start.js",
    "start": "npm run dev",
    "fix:laser-booktype:preview": "cd server && node scripts/fix-laser-book-type.js",
    "fix:laser-booktype:apply": "cd server && node scripts/fix-laser-book-type.js --apply --confirm=LASER"
  },
  "engines": {
    "node": ">=18 <21"
  },
  "dependencies": {
    "docx": "^9.6.1",
    "node-cron": "^4.2.1",
    "nodemailer": "^8.0.1",
    "pdfkit": "^0.17.2"
  }
}
