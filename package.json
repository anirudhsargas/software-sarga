{
  "scripts": {
    "build": "cd client && npm run build",
    "dev": "powershell -ExecutionPolicy Bypass -File ./start.ps1",
    "start": "npm run dev",
    "fix:laser-booktype:preview": "cd server && node scripts/fix-laser-book-type.js",
    "fix:laser-booktype:apply": "cd server && node scripts/fix-laser-book-type.js --apply --confirm=LASER"
  },
  "dependencies": {
    "node-cron": "^4.2.1",
    "nodemailer": "^8.0.1",
    "pdfkit": "^0.17.2"
  }
}
